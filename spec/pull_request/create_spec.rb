require 'spec_helper'

describe PullRequest::Create do
  it 'has a version number' do
    expect(PullRequest::Create::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
